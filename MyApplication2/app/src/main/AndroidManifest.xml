<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.grt.daemonw.myapplication">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-feature android:name="android.hardware.usb.host" android:required="true" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name="com.grt.daemonw.filelibrary.receiver.SdcardMountReceiver">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.MEDIA_MOUNTED" />
                <action android:name="android.intent.action.MEDIA_UNMOUNTED" />
                <action android:name="android.intent.action.MEDIA_REMOVED" />
                <action android:name="android.intent.action.MEDIA_EJECT" />

                <data android:scheme="file" />
            </intent-filter>
        </receiver>

        <receiver android:name="com.grt.daemonw.filelibrary.receiver.UsbMountReceiver">
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
                <action android:name="android.hardware.usb.action.USB_DEVICE_DETACHED" />
                <!--<action android:name="com.github.mjdev.libaums.USB_PERMISSION" />-->
            </intent-filter>

            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </receiver>

        <!--<provider-->
            <!--android:name="com.github.mjdev.libaums.storageprovider.UsbDocumentProvider"-->
            <!--android:authorities="com.github.mjdev.libaums.storageprovider.documents"-->
            <!--android:exported="true"-->
            <!--android:grantUriPermissions="true"-->
            <!--android:permission="android.permission.MANAGE_DOCUMENTS"-->
            <!--android:enabled="@bool/isAtLeastKitKat">-->
            <!--<intent-filter>-->
                <!--<action android:name="android.content.action.DOCUMENTS_PROVIDER" />-->
            <!--</intent-filter>-->
        <!--</provider>-->

    </application>

</manifest>