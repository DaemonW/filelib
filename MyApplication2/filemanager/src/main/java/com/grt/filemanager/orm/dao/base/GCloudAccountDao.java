package com.grt.filemanager.orm.dao.base;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import com.grt.filemanager.orm.dao.GCloudAccount;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.

/**
 * DAO for table "GCLOUD_ACCOUNT".
*/
public class GCloudAccountDao extends AbstractDao<GCloudAccount, Long> {

    public static final String TABLENAME = "GCLOUD_ACCOUNT";

    /**
     * Properties of entity GCloudAccount.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Account = new Property(1, String.class, "account", false, "ACCOUNT");
        public final static Property FeAccount = new Property(2, String.class, "feAccount", false, "FE_ACCOUNT");
        public final static Property Token = new Property(3, String.class, "token", false, "TOKEN");
        public final static Property RefreshToken = new Property(4, String.class, "refreshToken", false, "REFRESH_TOKEN");
        public final static Property TotalSize = new Property(5, Long.class, "totalSize", false, "TOTAL_SIZE");
        public final static Property LeftSize = new Property(6, Long.class, "leftSize", false, "LEFT_SIZE");
        public final static Property LoginTime = new Property(7, Long.class, "loginTime", false, "LOGIN_TIME");
    };


    public GCloudAccountDao(DaoConfig config) {
        super(config);
    }
    
    public GCloudAccountDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"GCLOUD_ACCOUNT\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"ACCOUNT\" TEXT UNIQUE ," + // 1: account
                "\"FE_ACCOUNT\" TEXT," + // 2: feAccount
                "\"TOKEN\" TEXT," + // 3: token
                "\"REFRESH_TOKEN\" TEXT," + // 4: refreshToken
                "\"TOTAL_SIZE\" INTEGER," + // 5: totalSize
                "\"LEFT_SIZE\" INTEGER," + // 6: leftSize
                "\"LOGIN_TIME\" INTEGER);"); // 7: loginTime
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"GCLOUD_ACCOUNT\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, GCloudAccount entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String account = entity.getAccount();
        if (account != null) {
            stmt.bindString(2, account);
        }
 
        String feAccount = entity.getFeAccount();
        if (feAccount != null) {
            stmt.bindString(3, feAccount);
        }
 
        String token = entity.getToken();
        if (token != null) {
            stmt.bindString(4, token);
        }
 
        String refreshToken = entity.getRefreshToken();
        if (refreshToken != null) {
            stmt.bindString(5, refreshToken);
        }
 
        Long totalSize = entity.getTotalSize();
        if (totalSize != null) {
            stmt.bindLong(6, totalSize);
        }
 
        Long leftSize = entity.getLeftSize();
        if (leftSize != null) {
            stmt.bindLong(7, leftSize);
        }
 
        Long loginTime = entity.getLoginTime();
        if (loginTime != null) {
            stmt.bindLong(8, loginTime);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public GCloudAccount readEntity(Cursor cursor, int offset) {
        GCloudAccount entity = new GCloudAccount( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // account
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // feAccount
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // token
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // refreshToken
            cursor.isNull(offset + 5) ? null : cursor.getLong(offset + 5), // totalSize
            cursor.isNull(offset + 6) ? null : cursor.getLong(offset + 6), // leftSize
            cursor.isNull(offset + 7) ? null : cursor.getLong(offset + 7) // loginTime
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, GCloudAccount entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setAccount(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setFeAccount(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setToken(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setRefreshToken(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setTotalSize(cursor.isNull(offset + 5) ? null : cursor.getLong(offset + 5));
        entity.setLeftSize(cursor.isNull(offset + 6) ? null : cursor.getLong(offset + 6));
        entity.setLoginTime(cursor.isNull(offset + 7) ? null : cursor.getLong(offset + 7));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(GCloudAccount entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(GCloudAccount entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
